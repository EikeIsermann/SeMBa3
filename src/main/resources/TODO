
- Look into simple text search. Should have very low performance at the moment?

- Improve metadata. Add XML Configuration to limit extracted metadata.

- Test ItemModification

- Put parent ontology definition in config.xml, create new TTL if library is empty

- Metadata type definition in Ontology and automatic value mapping for API classes


